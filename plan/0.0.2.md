# Plan: 0.0.2

The first plan in this tree, and the first artefact of the manager's seat. It
orders work across the seats and writes none of it: every item names the seat
whose diff it is, and a plan that says what somebody else must do is still a
plan and never their diff.

## The promise

0.0.1 published `@chbrain/kaal` with `files: ["bin"]`, so what a consumer
installs is the deterministic command surface alone. The package is the
primary one and carries everything an embedded consumer needs, which is the
tool and the skills and the agents together; sub packages are carved out
later, when a piece demonstrably travels on its own, and a consumer who takes
one is choosing to take less rather than being asked to assemble more.

So 0.0.2 promises three things a reader can check. Install the package and
you have the method, not a quarter of it. Open `tests/` and you find the whole
of it, five kinds, with every case sitting in the lane of the seat that owns
what it proves. And open any seat's lane and you can read what it needs from
which other seat before it can move.

## The re-evaluation: a working team is the objective

Re-measured on release head `bbd89e1`, after #315 and #312, both of which the
section below this one lists as open and awaiting review. This section
supersedes that cross-seat order; the accepted requirement it names is
unchanged, and #308 is moved out of this release by name further down.

The three promises above are met on `release`, and they are not the release.
The objective this release answers to is a working KAAL team: six seats that
can take work up, see what they are waiting on, and hand it across, and a seat
arriving cold that starts in the right place. Against the promises the tree is
done. Against the objective it is not, and two of the gaps are this release's
own decisions rather than things a later one discovers. They are placed here,
in 0.0.2, and the reason each is not moved is written beside it.

### The topology is declared three times, and the three disagree

`kaal gates` reports five cases blocked and names `architecture/*` as the lane
that owns every one of them. `kaal backlog` reports `nothing is blocked`.
`architecture/backlog.md` agrees with the second and knows nothing of the
five. The bug mechanism of item 4 and the backlog mechanism of item 8 both
landed in this release and they do not meet, so an architect reading its own
page learns nothing about work the board has already assigned it by name.

A backlog page states the principle that settles this: what a seat _can_ do
"is not here and never will be: that is a fact about the tree, so it is
derived rather than written". What a seat is blocked on is a fact about the
tree wherever the tree computed it, and five such facts exist today, typed
nowhere and therefore invisible. A topology that a seat must transcribe by
hand is not a topology the league runs on.

The manager's own page carries the same evidence from the other side. Under
`blocks:` it holds nothing, and beneath that a "Provisional lifecycle claim"
written in prose, because no persistent claim representation exists yet. A
mechanism being kept alive by hand in the tree it was built to mechanise is
the clearest statement available that the mechanism is not finished.

### Cold start is wrong on the branch a clone hands you

`AGENTS.md` on `main` says `Branch from main`. `AGENTS.md` on `release` says
``Branch from `release` ``. `main` is 99 commits behind, and `main` is what a
clone checks out, so the contract a new seat reads first is the stale one and
the branch it cuts is wrong before it has written a line. `README.md` on both
targets links to `DESIGN.md` and to nothing else: no route to `SURFACE.md`, to
`AGENTS.md`, or to this file.

No wall reaches this and none can, because `main` lagging `release` is the
design rather than a defect. The cost is not theoretical: a session oriented
itself on `main` today, read the stale contract, and produced a wrong reading
of the whole tree from it before the divergence was found. `a-reader-can-find-
the-authority` was stated from a cold read that failed the same way, and it
names the same absence: the information was present, its authority and route
were not.

This belongs here rather than in 0.0.3 because the promotion is the act that
first makes `main` and `release` agree, and a release that ships a front door
pointing at the wrong branch has shipped the trap rather than the method.

## Correction on the accepted common lifecycle

> **Superseded, and kept only for the decision it records.** Everything in this
> section about live state was true of release head `de94ab8f` and is not true
> now. In particular: #315 and #312 are described below as open and awaiting
> review, and both are merged, #312 being the current release head `bbd89e1`;
> the cross-seat order numbered below is replaced by the order in the items
> above; #308 is moved out of this release by name further down; and the board
> it quotes, fifteen walls with five failing, has since been three failing with
> five blocked. Read the section above and the items for current state, and
> read this one only for the requirement it accepts, which stands.

Re-measured on the exact remote `release` head
`de94ab8f2dea71944b064288adffaf2eccf996cf`, after #314. The repaired
`retro-4ls` naming surface now reaches the hundredth use, and Analyst draft
#312 has resumed. The merged release tree is byte-for-byte the tree whose
successful #314 CI run reports fifteen walls, five failing and none waived.
All six backlogs still carry no blocks, while seven pull
requests are open and their lifecycle states show ready, in-flight and
changes-requested work. The backlog reader therefore still says nothing is
clear yet: empty block maps do not make live work visible.

`a-seat-claims-its-work-before-doing-it` is accepted into 0.0.2. It narrows
the backlog promise already in this plan: a seat must be able to state its
intended work as well as what it cannot do, and another reader must be able to
distinguish a draft claim from delivered repository truth. Reconciliation,
the version, the release record and the cut wait behind that lifecycle.

The accepted requirement deliberately chooses no persistent claim encoding,
provider adapter, wall, workflow or skill. Kai's later ask is that the common
cycle live in one portable `pdca` skill rather than seven seat skills.
That ask is not implementation authority and is not placed here. It goes to
the Analyst, who may admit, split, link, await or refuse it before any Manager
release decision.

Until that capability exists, a session bootstraps the cycle manually in its
own pull request: draft container and exact backlog claim before substantive
work, then proof, target board, retro and reconciliation before ready.

The current cross-seat release order is:

1. Tester PR #315 is the first clear seat result. It is ready with successful
   CI, reconciles the trace, suite and record evidence, and records the five
   remaining proof failures as Architect-owned bugs. Landing it releases a
   smaller board with named ownership instead of leaving the same failures as
   unstructured evidence.
2. Analyst PR #312 is the second clear seat result. It is ready on the current
   release base with successful CI and carries the reusable external-control
   procedure that must precede the separate release-specific verification
   record. The Analyst's local backlog owns its order relative to the other
   open Analyst lanes.
3. Architect follows the bugs made durable by #315 and, after #312 lands, its
   accepted requirement. Draft #308 remains changes-requested and unassigned,
   so it does not create a Developer handoff for 0.0.2. The Architect's local
   backlog owns the order inside that lane.
4. Developer follows accepted drawings. Operator follows current proof, the
   external-control procedure and the separate release-specific verification
   record.

The immediate human gate for this ordering is review and merge of this
Manager plan. After it lands, #315 is the first seat result to review for
merge, followed across seats by #312. The later gates remain the other
approved merges, external control answers, the version move, the deployment
key and the manual release dispatch.

## What closed

Twelve items were ordered here and are merged. They are kept by name because the
order they were done in is the only record of why the later ones exist.

1. Declare the manager and the operator, so `deploy/` has an owner and this
   file has a lane. Governance.
2. Record the runs that were owed then. Tester. The cheap step that made items
   5 and 6 visible at all.
3. Draw `an-install-carries-the-method`. Architect.
4. Build it: `bin/lib/assemble.mjs`, the `assemble` command, `SURFACE.md`.
   Developer.
5. Declare what shipping the method supersedes, across three closed tasks.
   Analyst.
6. Teach one contract that an exclusion is not a path, and amend the other
   under that supersede. Architect, two diffs.
7. Ship the method: `files` carries `bin`, `skills` and `agents` and excludes
   the tests, and the page names the step. Governance.
8. Route every pin the release claim rests on. Whichever seat owned the pin.
9. Record what is already proved. Tester, twice: five records, then a sixth
   diff for the one two merges left stale. It found that
   `an-install-carries-the-method` had never run on Windows, which the
   analyst then fixed in its own lane.
10. State the suite layer. Analyst. `a-suite-names-its-cases`, seven criteria,
    seven red tests.
11. Draw it and build it. Architect, then developer, then the tester's record.
    Ten seams, a trace that may be written as a block where a comma list will
    not fit, and a plan that stopped owning a place and a number.
12. The four diffs the record found. Writing one line of evidence turned up a
    fixture proving the wrong cardinality, a wall rule a refactor had dropped
    with nothing saying so, twelve fixture pages a later rule overtook, and a
    report saying `green` about a suite it never opened. Four lanes, and not
    one of the four was found by a wall.

## The delta

Measured on merged main.

- Fourteen walls green, none failing, none waived.
- Coverage: analyst 65 of 65, architect 58 of 65, tester 63 of 65. The two the
  tester is missing are `a-suite-names-its-cases`, which is stated and
  unbuilt, and `push-v1`, which is red on criterion 7 and blocks nothing here.
- `tests/` holds four of the five kinds it should and one thing that is none
  of them: a strategy, three plans, sixty-three runs, and twenty-one
  `.test.mjs` files. There are no suite documents and no bugs.
- Those twenty-one are the developer's subject in the tester's lane. Twenty
  import `bin/lib` directly and the twenty-first drives the CLI. No wall can
  catch it: the seats guard's proof table is shaped `<tree>/<task>/<proof>`
  and a unit belongs to a module, so there is no row to add.
- What CI re-runs is not in `tests/`. It is globs inside gate commands in
  `kaal.config.json`, which is the governance lane, so the tester cannot
  change what is re-run without a diff in somebody else's lane.
- No seat can say what it needs from another seat, and the lane guard, which
  refuses a crossing several times a day, computes the owning seat one line
  above the finding and then discards it.
- Nothing in the tree can see the registry scope, the publish credential, or
  the repository's merge protections, and `public-v1`'s Handoff has been
  carrying a list of them, unread by any wall, since it was written.

## What a seat needs from a seat

The table is doctrine and belongs in `AGENTS.md`: it is the same on every
task, it names the only question one seat may put to another, and it is what
keeps an ask inside a lane instead of becoming an edit in someone else's
tree. What changes per task is which of these edges is live, and that is what
a seat's own plan records.

Each seat's work exists only when a specific upstream artefact is in a
specific state, and is blocked by a state it cannot change itself.

| seat      | work exists when                                                 | blocked when                                             |
| --------- | ---------------------------------------------------------------- | -------------------------------------------------------- |
| analyst   | an ask has no requirement, or a closed criterion is contradicted | only a person can answer                                 |
| architect | a requirement has no drawing                                     | the criterion cannot be drawn as written                 |
| developer | a test is red                                                    | the red test needs a seam that does not exist            |
| tester    | a criterion has no proof, or a proof has no fresh record         | a verify or validate fails for a reason outside `tests/` |
| operator  | something is proved and unshipped                                | a control outside the tree                               |
| manager   | two seats are clear and the order is not obvious                 | never on its own account                                 |

The manager cannot be blocked by a seat, only by proxy, carrying somebody
else's block. That is why it is the seat that picks blocks up, and it is the
argument against seats handing work to each other directly.

Three rules hold the whole of it:

- **A seat names the block and never the fix.** The blocked seat says where,
  the manager says when, the owning seat says what. None of the three may
  decide for another, which is why neither a block nor the manager's pick-up
  carries a field for a remedy.
- **A refusal is a block.** Every time a seat tries to write outside its lane,
  that is the moment to record one, and the wall that refuses it already knows
  which seat owns the path.
- **A bug blocks its retest.** A failing verify or validate is recorded as a
  bug naming the earliest of requirements, architecture, code or operations
  where it can be fixed, and the case it blocks is not retested until it
  clears.

## The order

### 1. A promotion names what it refuses

- Delta: the asker's rule is that the tester may turn a target red and CI may
  not, and one target cannot be both. Today there is one, so the licence a red
  test already has (`not delivered` is an answer and the wall stays green)
  lives on the same branch a consumer installs from.
- First rather than last, and that is this revision. The asker's words are
  that branches reach a release branch and release reaches main never red,
  which is wider than the two verdict gates below: it is the whole board, and
  it says where a red one may stand. Item 2 cannot be done without it. A unit
  moving beside its module is a file the developer adds and a line the tester
  writes naming it, the suites wall reports a test file no suite names, and
  the two are two lanes. File first is a red board and line first names a file
  that is not there, so on one target the move is impossible and on two it is
  a red release branch for one merge. Twenty-one times.
- Seat: analyst, then the chain, then governance for the workflow, and Kai for
  the branch and its protections, which are item 9's kind of control.
- Needs: nothing in the tree. It rests on `kaal runs`, which is stable, and
  not on the suite layer.
- Action: a release branch between, and two gates rather than one. A branch
  reaching release refuses `regressed` and `nothing ran`, which both mean a
  claim that was true and is not. Release reaching main additionally refuses
  `not delivered`, which is the one a tester is allowed to leave standing. The
  strict gate is late rather than early on purpose: red is allowed to exist
  and is refused publication, rather than kept out of the tree where nobody
  sees it.
- And the board itself is judged the same way round. A wall may stand red on
  `release`, where the seat that can fix it is still working and can see it.
  No wall may stand red on the promotion to main. So a diff that leaves a wall
  red is a diff that may be merged and may not be published, which is the
  licence the verdicts already give a task and the board has never had. A wall
  red on `release` is not a licence to leave it red: it is a block, with an
  owner, and the promotion is what refuses to forget it.
- Evidence: a tree carrying one `not delivered` task reaches release and is
  refused main, naming the task. The four verdicts already compute this, so
  the branch is a place and a rule rather than a mechanism.
- Three of the four protections are walls rather than settings. That main
  accepts only from release is a check reading the pull request's head branch,
  deterministic and offline and in the tree; so are both verdict gates, which
  are `kaal runs` plus a condition on the base branch. What must be a setting,
  and is therefore Kai's: a pull request required on `release` and on `main`
  with no direct push, force push and deletion blocked on both, and the checks
  marked required so a red one stops a merge. Four rows in a ruleset, joining
  item 9's list rather than becoming a new kind of problem.
- Carries a governance diff that must land the same day: a lane branch is cut
  from `release` and no longer from `main`, which is a sentence in `AGENTS.md`
  and in `CLAUDE.md`. Written a day late, the next branch comes off the wrong
  base and the first merge is a conflict nobody caused.
- Each piece starts at the rung it can reach and is named with where it is
  going, because the ladder runs human, then the pages, then a skill, then a
  wall. The two verdict gates start at the wall, since `kaal runs` already
  computes them and a thing that can be a wall today has no business being a
  sentence. The branch a lane is cut from starts as the pages and hardens once
  a diff is refused for coming off the wrong base. The four ruleset rows start
  at the human and stay there until the tree can read them, which is item 9's
  standing complaint and not this item's to solve.
- Unblocks: item 2, which cannot be split across two lanes without it, and
  item 13, which is the first promotion this rule judges.

### 2. Move the twenty-one beside the modules they import

- Delta: twenty-one of twenty-five units sit in the tester's lane over the
  developer's code, and the suite layer makes that unnameable rather than
  merely wrong.
- Seat: tester and developer, and it cannot be one diff. A path leaving
  `tests/**` for `bin/**` is two lanes and the guard refuses it, so this is
  the first real instance of a block rather than an example built for one.
- Needs: item 1, because the middle of this move is a red board and there is
  nowhere for it to stand today. And the manager to pick the block up, and the
  two seats to take their own halves.
- Action: add beside the code first and delete after, so there are duplicates
  for one merge and never a hole. Deleting first leaves a green board holding
  twenty-one fewer units, which is worse than red.
- Evidence: `tests/` holds no `.test.mjs`, the units wall runs the same count,
  and no suite names a case under `tests/`.
- Unblocks: nothing, and it is the thing the release is for.

### 3. The regression plan CI runs

- Delta: what is re-run is declared in the governance lane, in globs inside
  gate commands, so the seat that owns the method does not own the selection.
- Seat: analyst, then the chain.
- Needs: the suite layer, which is closed, because a plan cannot pick a suite
  before a suite exists.
- Action: a plan that selects verification suites across the other plans, and
  the board reading it rather than implying it. Verification only, because a
  wall cannot grade what an eval judged; that line is about what a wall may
  read and never about what is worth testing.
- Evidence: one suite named by two plans, and CI running what the plan says.
- Unblocks: item 11, which is what a release is cut against.

### 4. Bugs, the fifth kind

- Delta: a failing case has nowhere to be recorded and no owner.
- Seat: analyst, then the chain.
- Needs: the suite layer, which is closed.
- Action: `tests/bugs/` beside `tests/runs/`. A bug is the run record
  inverted: a run says a suite passed and goes stale when the suite moves, a
  bug says a case failed and clears when the case goes green. It names the
  earliest lane where it can be fixed and never the fix.
- Evidence: a bug over a green case is a finding, and the blocked case is not
  retested while it stands.
- Unblocks: item 6.

### 5. The refusal names its block

- Delta: the lane guard refuses a crossing several times a day, computes the
  owning seat one line above the finding, and throws it away.
- Seat: analyst, then the chain.
- Needs: somewhere for a block to land, so item 4 or item 6 first.
- Action: the finding names the seat that owns the path and says the remedy is
  a block. This is the whole negotiation trigger and it is a message.
- Evidence: a crossing answers with an owner and a place to record it.
- Unblocks: item 6.

### 6. The dependency table, and a backlog in every lane

- Delta: the table above is written here and nowhere a wall can read it.
- Seat: analyst, then the chain, then governance for the table in `AGENTS.md`.
  No shared path is declared and none is needed, which is the change from what
  this item said before.
- Needs: items 4 and 5, which are where blocks come from.
- Action: each seat carries a backlog in its own tree, `backlog.md` beside
  what that seat already owns, and writes its blocks there. The manager reads
  all six with a script and prioritises across them; it collects and never
  edits, which is the sentence `AGENTS.md` already has about a block being
  picked up as it stands. The entry shape is flat and keyed by task, the shape
  the `reviews:` block already proved under the same parser, and the self
  clearing rule holds, so a seat writes a block once and never maintains it.
  The word is the asker's: `plan` is the manager's page and the tester's page
  for a wall, and a third meaning one word apart from `tests/plans/` would be
  a hazard rather than a name.
- Why the backlog is in the lane and not in one shared directory: a shared
  page is what two sessions collide on. Six backlogs under `plan/seats/` are
  six files in one tree that every seat may write, which is a conflict on
  every merge; six backlogs in six trees are never touched twice. That buys
  four things the asker named. A seat lands everything it is not blocked on,
  its backlog included, in one pull request. The manager prioritises across
  without opening a seat's tree. No seat reaches outside its lane to say what
  it needs. And sessions can run in parallel, because two seats working at
  once write no file in common.
- What serialises them today is a defect and not a constraint, and it is the
  asker's correction. Every seat's cases already live in its own lane: an
  acceptance case with its requirement, a contract with its drawing, a unit
  beside the code. What does not is the list of them. `tests/suites/*.md`
  carries a hand written `cases:` block with a sha per path, so a seat that
  creates a case file reaches into the tester's tree to add a line, and that
  edit moves the pin on every plan naming the suite: the acceptance suite
  moves two plans now that regression names it too. Two branches in one
  session hit exactly that conflict twice this release, both on a suite
  gaining a case.
  Measured, so the size of it is known. Changing a case file a suite already
  names costs nothing: the pin reads `review-needed` and `kaal traces` exits 0. Adding one is a finding: a new file under a tree the suites reach answers
  `no suite names it` and the wall exits 1. So the analyst and the architect
  pay it once per task by definition, and the developer only when a module is
  new.
  The asker's shape is that collecting is deterministic. A suite is computed
  from what the tree holds rather than listed by hand, so nobody writes into
  `tests/` but the tester, and what the tester writes there is the selection
  and the grouping, which is the judgement, on top of the local management
  each seat already does in its own lane. Then no seat reaches outside its
  lane at all, no pin ripples from a case being born, and parallel sessions
  stop needing an answer rather than getting one. That is a task of its own
  and this item is not it.
- And a smaller thing the measurement turned up: `review-needed` fails
  nothing. A tree can carry unread pins for ever and every wall stays green,
  which is the shape this release has met three times in other places.
- Evidence: `kaal plan` prints a clear set and a blocked set grouped by the
  seat that owes it, read from six backlogs and written into none.
- Unblocks: items 7 and 8.

### 7. The manage skill

- Delta: ten retro records point at a question no skill asks.
- Seat: none, the skill lane.
- Needs: item 6, because a skill that asks for a shape nothing reads is a
  ceremony.
- Action: two questions a seat answers when it puts work down: what do I need
  and from whom, and what may I go do now.
- Unblocks: item 8.

### 8. Six seats write their first backlog

- Delta: the mechanism exists and holds nothing.
- Seat: all six, one diff per lane.
- Needs: items 6 and 7, and item 6 needs this one back. The mechanism landed
  without the line that keeps a seat's page out of a published tarball,
  because `the-engine-is-installable` refuses an exclusion in `files` that
  matches nothing, and `!**/backlog.md` matches nothing until a seat has
  written one. So criterion 7 of `a-seat-carries-its-own-backlog` cannot
  close before this item opens, and the exclusion is a governance diff that
  follows the first page rather than preceding it. Two closed criteria,
  neither wrong, compatible in one order only, and neither page says so.
- Evidence: the manager's next plan is a sort over six backlogs rather than a
  memory, and the sort is a script rather than a reading.
- Unblocks: item 10, and the last criterion of item 6.

### 9. Read the controls that are not in the tree

- Delta: registry scope, publish authentication, merge protections, and the
  list `public-v1` has been carrying in prose.
- Seat: operator to gather, and the answer is Kai's.
- Needs: from the manager, the answers. No wall reports this and none can.
- Unblocks: item 13.

### 10. Reconcile on merged main before the bump

- Delta: counts and records are written per branch and merged in pairs, and
  main has carried a stale record twice this week, once today.
- Seat: tester for the records.
- Needs: items 2 and 8 landed.
- Action: `kaal traces --write` and `kaal runs --write` on merged main, as
  their own diff, landing only what is owed.
- Unblocks: item 11.

### 11. The version

- Delta: `package.json` says 0.0.1.
- Seat: governance, and the line is Kai's to move.
- Needs: item 10.
- Unblocks: item 12.

### 12. The release record

- Delta: there is no `deploy/releases/0.0.2.md`.
- Seat: operator, its own diff, because the version is governance and the
  record is evidence of an act.
- Needs: item 11.

### 13. Cut it, and say what it is

- Delta: nothing published.
- Seat: operator.
- Needs: items 1, 3 and 8 through 12, and the dispatch is Kai's.
- Action: the regression plan green on main, the human's dispatch, tag,
  publish, check visibility, smoke by name in a clean tree, record the proof,
  operate retro.
- Evidence: an installed package answering by name outside this checkout.
- Unblocks: 0.0.3, which opens with the relationship between architecture and
  requirements, because architecture is very nearly a mirror of requirements
  today and a mirror is not a tree. The suite layer's many to many and the
  case references beyond what the suite layer needs go there too, and so do
  the three wants the asker has placed there, below.

### 14. The regression standing on `release`

- Delta: `a-seat-claims-what-it-covers` criterion 6, "the board runs it, and
  its line carries the counts", passes at `2a28e09` and fails at `9d24f0a`.
  Both were run to establish it. The acceptance wall reports the verdict as
  `regressed`, which is the one item 1's own rule refuses at the entry to
  `release`, and it is standing on `release`.
- Seat: tester, and no further than that. A failing verify is recorded as a
  bug naming the earliest of requirements, architecture, code or operations
  where it can be fixed, and the seat that owns the lane the bug names is the
  seat this item belongs to. Naming it here would be the manager saying what
  rather than when, and the board's line being read by a developer case is
  where the symptom surfaced rather than a finding about where the cause
  lives.
- Needs: nothing.
- Nobody did anything wrong, and that is the finding. #315 touched `tests/**`
  and nothing else, a single lane diff doing exactly what the method asks. It
  added five bug pages, the board consequently began printing `bugs: <case> is
blocked, and architecture/* owns it`, and a developer case that reads the
  board's line broke. The board's output is a contract that nothing declares,
  so the tester could not have seen it coming and no wall caught it on the
  way in.
- Evidence: the case green on the release head, and the acceptance wall
  reporting no `regressed` verdict.
- First, because a verdict the rules refuse is not a thing to carry while
  other work is chosen.

### 15. One answer to "am I blocked", whoever asks

- Delta: the two declarations above, disagreeing. Five blocked cases with an
  owning lane named by one command and unknown to the other.
- And a third declaration, found while writing this item. The acceptance wall
  answers `not run: a standing bug blocks it` for
  `requirements/applies-here/acceptance.test.mjs`, and the regression wall
  answers `case: requirements/applies-here/acceptance.test.mjs is red` for the
  same file in the same tree. `architect-v2` reads the same way. One wall
  honours the bug pages and the other has never heard of them, so a seat's
  answer to "am I blocked" depends on which wall it asked. This is the
  contradiction already named under the risk this release carries, standing on
  Linux and for a different reason than the one named there: not two routes to
  a case, but one mechanism read by one wall of the two that reach it.
- Seat: analyst, then the chain.
- Needs: item 14 out of the way, because the board's line is the surface both
  of these read.
- Action: not this file's to say. The three declarations disagree; which of
  them is authoritative, and whether the others read it or are withdrawn, is
  the shape of the thing and the shape belongs to the seat that owns it. A
  manager naming the mechanism here would be deciding for the analyst and the
  architect at once, which is exactly what the three rules below this plan's
  dependency table forbid: the blocked seat says where, the manager says when,
  the owning seat says what. This item says when, and it says it is now.
- Evidence: one case, asked of every surface that can answer, giving one
  answer. Whether that is a page, a command or a wall is the answer to a
  question this item is not allowed to settle.
- Unblocks: the manager reading blocks off the seats rather than off a board,
  which is what item 8 was for and has never done.

### 16. A seat starting cold establishes where it is before it reads anything

- Delta: a route through pages is the smallest part of this and was all the
  first draft of this item asked for. Orientation is four facts, and a cold
  session today can get all four wrong while believing it has them.
- The four, each with the way it failed in this league rather than in the
  abstract. **The authoritative remote target**: a session read local refs,
  found two, and reported that no `release` branch existed. There were eighty
  four branches on the remote and `release` was one of them, ninety eight
  commits ahead of the target being read. **The exact base the reading is
  against**: the same session measured a board, coverage rows and an open
  order against `main` and reported all three as current, with nothing in the
  reading naming the sha it was true of. **An existing pull request for this
  seat**: a lane can be opened a second time by a session that never looked,
  and the recovery is to find the seat's open one before cutting a branch, not
  after. **An honest unknown**: the first of these was reported as a fact and
  not as a thing unchecked, which is the failure that makes the other three
  expensive, because a wrong answer stated plainly is acted on and a missing
  one is looked up.
- Seat: analyst for `a-reader-can-find-the-authority`, which is stated from a
  cold read that failed the same way and needs a base merge; then the chain;
  then governance for whatever the route turns out to be, and the promotion is
  the act that puts it where a clone will read it.
- Needs: nothing in the tree.
- Action: not this file's to say, beyond one constraint the manager may set
  because it is about where and not what: whatever is stated must be true on
  `main`. A route correct only on `release` is a route the cold reader never
  reaches, because a clone checks out `main` and `main` is the stale one by
  design.
- Evidence: a session that has read nothing else can say which remote target
  is authoritative, which sha its reading is against, whether its seat already
  has a lane open, and which of the four it has not established. The fourth is
  the one to test hardest: a tree that cannot answer `I have not checked` will
  answer something else.

### 17. The five bug pages, and what clears them

- Delta: `applies-here` twice, `architect-v2` twice, `nothing-passes-
vacuously` once, recorded as bugs by #315, all five naming `architecture/*`,
  all five in tasks this tree calls closed. A closed task's red is a failure
  and not a report.
- Possibly overtaken, and on a branch that may not land. #319 reports all five
  cases passing on its branch and its `traces` wall red on exactly those five
  pages: a bug is cleared by its case going green, and five pages would then
  describe cases that do not fail. That reading holds only if #319 lands as it
  stands, and #319 is a draft the supervisor has blocked, so the five pages
  describe cases that are still red on `release` and the tester has nothing to
  remove yet.
- Seat: tester, and only once item 18 clears. `tests/bugs/**` is not a shared
  path, so no other lane may take the records out whatever its board says.
- Needs: item 18, which is blocked.
- Evidence: the board carrying no blocked count and the `traces` wall green.

### 18. The security gate, finished rather than half standing

- Delta: `an-open-finding-blocks-every-target` had six criteria passing, eight
  failing, and a drawing missing criteria 6 through 14 from its strategy
  table. It is the gate that binds a finding to an exact candidate, so a
  gathered clean snapshot stayed green while the repository moved under it.
- Placed in 0.0.2 by the asker, and this revision records the decision rather
  than the question it first asked. Shipping a half built security gate is
  worse than finishing it: half a gate reports green about the half nobody
  built, and the half nobody built is the half that notices the candidate
  changed.
- Seat: architect, and no seat after it is named yet. #319 is the architect's
  diff, it is a draft, it is behind the current base, and the supervisor has
  blocked it with changes required. It carries the requirement from five
  criteria to fourteen and five seams to eight with a contract test each, and
  its `drawings` wall is green where the release head's is red; none of that is
  the architecture being settled, and an earlier revision of this file said the
  cost was mostly paid, which was a plan reading a diff's own summary rather
  than its review.
- The block, in the supervisor's terms: the provenance boundary is
  self-asserted, so criterion 13 is not satisfied. A person can copy the
  schema 2 shape, compute the manifest locally, declare `retrieval.state: ok`,
  `analysis.state: completed`, `collection.state: complete`,
  `collection.locations: all`, `collection.expected: 0` and `findings: []`, and
  the offline wall has no independent fact against which to check that any scan
  ran. Criterion 9 has the same hole from the other end, because a truncated
  collector declares its own truncated length as the expected one. The current
  criterion 13 proof rejects a hand written schema 1 snapshot and never tries
  the hand written, internally consistent schema 2 one this design would
  accept.
- Needs: the block cleared, and the way it clears is not this file's to pick.
  The supervisor named two: a root of trust the wall can authenticate without
  the producer's word, or an amendment to the requirement if authenticity was
  never intended. The second is an architect to analyst return, which is the
  architect's own blocked-when in the table above, the criterion cannot be
  drawn as written. Either way the analyst may be owed a diff before the
  architect is, and the manager names neither.
- Not handed onward. No developer work is ordered by this item while the
  drawing is blocked, whatever the diff's own handoff offers, because a seam
  built against a drawing that may be withdrawn is a seam built twice.
- Evidence: fourteen criteria green, forged schema 2 clean evidence and
  internally consistent truncation both red, and the gate declared in
  `kaal.config.json`, which #319 says it does not do and leaves to governance
  behind criterion 14.

## What this revision moves out of 0.0.2, by name

A want that leaves a release without being named has not been placed, it has
been dropped, and the reader cannot tell the two apart later. Each of these is
stated here with the reason it is not required for a working team, and each
joins the section below as an ask the analyst has already judged or will.

- **`retro-4ls-counts-what-it-feeds`**, stated and neither drawn nor built,
  nought of ten criteria green. It counts what the league learns; it does not
  make the league work. It leaves with a number attached rather than a
  shrug: unconsumed stands at 55 for `analyse`, 39 for `code`, 37 for
  `architect`, 18 for `test` and 4 for `manage`, which is 155 across five
  skills against a rule that fires at ten. The rule is being read past, and
  the entry below already carries the asker's ratchet as its shape. Moving the
  task does not move the debt.
- **`a-maintainer-can-see-where-board-time-is-spent`**, one criterion red,
  drawing #308 in draft and unassigned. Instrumentation of the board is worth
  having and no seat is blocked without it.
- **`an-agent-skill-keeps-what-makes-it-work`**, #307, ready and seven behind.
  It is a promise to a consumer who takes one skill out of the tree, adjacent
  to the method shipping whole, and no seat inside the league waits on it.
- **`a-human-ask-earns-its-requirements`**, #306, ready and seven behind. This
  is the closest call of the four and it is decided rather than deferred: it
  governs the edge from a person to the analyst, where the two decisions kept
  above govern the edges between seats. `AGENTS.md` already carries the
  doctrine that an idea reaches the manager through the analyst, nothing in
  0.0.2 depends on the evidenced version of it, and a working team is the test
  this release is being measured by.

## The decision the asker took

The revision before this one put the placement of
`an-open-finding-blocks-every-target` to the asker rather than deciding it,
because finishing it and withdrawing it are both defensible and only leaving
it half standing is not. The answer is to keep it in 0.0.2, and it is item 18
above. The reason given is the one that settles it: a half built security gate
is worse than a finished one and worse than none.

The asker's recommendation also said #319 had paid most of the remaining
architecture cost, and this revision of the file does not repeat it, because
the supervisor has since blocked that diff on the provenance boundary and a
blocked drawing has not paid anything yet. The decision to keep the task in
0.0.2 is unaffected and is the asker's; what changed is the cost, which is now
unknown rather than mostly spent, and an unknown cost is a worse argument for
keeping a thing than a small one. It is kept anyway, on the first reason
alone, which is the one that did not depend on the estimate.

## Placed for 0.0.3, by the asker

Wants placed in a release by the person who owns the priorities, and they are written here rather than left in a conversation
because a want nobody can read is a want nobody can plan. None of them is
stated yet: each is an ask, and the analyst judges it into a requirement with
a proof or refuses it with the reason, which is what `AGENTS.md` now says an
idea does before it reaches this file.

- **The league learns while it moves.** Ten unconsumed retros on one skill is
  the analyst's next ask, the rule is in `skills/analyse/SKILL.md` and in
  `AGENTS.md`, and nothing enforces it: `kaal retros` answers `analyse: 41
unconsumed` and exits 0. What the four lenses produce is the engine's own
  improvement, and what survives the analyst's reading is a requirement no
  plan names yet, which is the backlog and needs no sixth kind of page. The
  asker's design is a ratchet rather than a line at ten: red where unconsumed
  grows, or above a starting number each release lowers. A wall that fires at
  ten today would be red on four skills at once with ninety-six retros between
  it and green, and a wall nobody can satisfy is a wall people read past. It
  bites as every wall bites, red on `release` and refused at the promotion,
  and not by refusing a lane's entry to `release`: that would couple every
  seat's throughput to the analyst's queue, which is the coupling the lanes
  exist to avoid. The asker's words, and they are the shape of the ask rather
  than its criteria.
- **The release and the regression plan are two plans, and regression is
  derived.** `main` is the current release and `release` is the next one.
  `main` enforces both plans at once; `release` enforces regression as a hard
  wall and lets the release plan's wall stand red, which is the licence. So a
  wall binds or does not bind per wall and per target, where today `binding`
  is one boolean per target, and that is a sixth seam over the five
  `a-plan-picks-its-suites` draws.
  What regression holds is not last release's release plan. A release changes
  requirements as well as adding them, and a changed requirement's old cases
  are exactly the ones that must not be enshrined: the behaviour they hold was
  deliberately replaced. The tree already carries that distinction and nothing
  reads it for this. A requirement that supersedes another is a change; one
  that supersedes nothing is an addition. So regression has a definition and
  not a list: the cases of requirements delivered in a released version and
  not superseded since, which drops a requirement the moment its replacement
  is stated.
  Derived rather than written, because derived is deterministic, and that is
  the ladder rather than a change of mind: a plan naming its suites by hand is
  the pages rung, and a selection the board computes is the wall. 0.0.2 builds
  the first; this builds the second over it. What survives the move is every
  reader `a-plan-picks-its-suites` draws, because only where the set of suites
  comes from changes.
- **A target that moved alone says so.** `main` can move without `release`, by
  a hotfix, and nothing in the tree reads the pair or says which way the sync
  goes. The asker's shape is a pull request from `main` into `release` where
  they are out of step. It is a different question from a promotion being
  asked for, which is why it is not a criterion of
  `a-promotion-names-what-it-refuses`.
- **A seat says where it is blocked, and never answers for the seat it
  names.** The asker's briefing states it as one sentence: a seat may say what
  it needs from another seat, and it may not manufacture that answer inside
  its own pull request.
  The mechanism is items 6, 7 and 8 of this plan and not a new thing. A block
  sits in the seat's own backlog, in the seat's own tree, the manager reads all
  six with a script and prioritises across them, and the manage skill is what
  asks a seat the two questions when it puts work down. Item 6 already fixes the entry shape,
  flat and keyed by task, and the self clearing rule so a seat writes a block
  once and never maintains it, which is the briefing's fifth question answered
  before it was asked. `kaal plan` printing a clear set and a blocked set
  grouped by the seat that owes it is the briefing's legibility, stated as
  evidence rather than as a hope.
  Two more halves of it are shipped. One pull request is one lane, declared on
  the branch and enforced by the seats wall, so a diff reaching another seat's
  path is already refused and the briefing's second acceptance bullet exists.
  And `tests/bugs/`, built this release, is where a block comes from at one
  scope, which is what item 6 means by needing items 4 and 5 first.
  So what is left for 0.0.3 is the part items 6 to 8 do not state. The five
  fields a block carries, where item 6 fixes only that the shape is flat and
  keyed by task. The rule that the reporting seat may not close a block whose
  subject is another seat's, which is adjacent to the self clearing rule and
  not the same: one is about who maintains an entry and the other about who
  may declare it answered. The smallest stable identity for a block's subject,
  where the tree already has an answer to borrow, because a trace pin is a
  name and a sha over a region with a review line saying who read it and what
  moved. And whether a block refuses anything at all, which is the briefing's
  second question.
  On that last one, weigh the briefing against what this release learned. It
  says the promotion refuses unresolved blockers. A red wall on `release` is
  reported while the check goes green, so a thing that only bites at the
  promotion accumulates unseen until the cut, which is the worst moment to
  meet it; that is why a standing bug keeps the board from green rather than
  waiting for `promote`. A block on a plan is a weaker claim than a red case
  and may deserve a weaker answer, and the question is worth asking in those
  terms rather than in the briefing's.
- **The evals could run.** `evals.yml` reads `EVALS_API_URL`, `EVALS_API_KEY`
  and `EVALS_MODEL` and refuses in one line when they are missing, and the
  reason they are missing was the cost of the calls. The asker reports a
  runtime where that cost is near zero. Two models on one fixture took an
  afternoon by hand this week; every fixture on every skill is a workflow run.
  That is what would make a move's evidence its own rather than its skill's.
- **The tree ships packages and consumes them, and the two places are one
  place today.** The asker's shape: using a package is the operator's to
  provide, creating one takes the whole team, and the developer owns where
  the code that becomes a package is written, which is not where the backlog
  about using it sits. Two places, one of them source and the other what is
  produced, consumed and then built on.
  Measured, because the boundary is already crossed and nobody named it.
  `package.json` packs `bin`, `skills` and `agents`; `bin` is the developer's
  tree, `skills` is the skill lane's and `agents` is the agent lane's. So
  what this repository publishes spans one seat and two seatless lanes, and
  nothing in the tree says which paths are the package and which are the
  league that uses it. The distinction exists one layer up already, in the
  asker's own words: there is delivery architecture, what we use to deliver,
  and solution architecture, what we deliver. This is that line drawn through
  the file tree rather than through a drawing.
  What it dissolves is a patch. `a-seat-carries-its-own-backlog` criterion 7
  exists only because one of the six seat trees is published: a developer's
  backlog at `bin/backlog.md` would reach every consumer, so the manifest
  excludes it. Split the source of the package from the tree that consumes
  it and the exclusion has nothing to exclude, because no seat's working page
  is inside what ships. The criterion is the right answer to the tree as it
  stands and the wrong answer to the tree the asker is describing, and it
  says so here rather than being quietly carried.
  A name is wanted and the asker says so. `source` names the first half and
  leaves the second unnamed, which is the harder one: it is the league using
  its own engine. `engine` and `league` are this tree's own words for exactly
  those two things, and naming is the analyst's rather than the manager's, so
  they are offered and not chosen.
  The second half is the asker's and it is the sharper half: the league
  consumes the published package and never the working tree. A produces B,
  and after B is released the league moves to B and B produces C. The bump is
  the operator's act by the asker's first rule, and it is the first act of a
  release rather than the last.
  What that buys is the thing this release has met four times and patched
  each time: **the judge is outside the diff**. Today a wall and the tree it
  judges are in one diff, so a weakened wall passes its own weakening, which
  is why every seat here reads its own diff adversarially and why a stand-in
  is discipline rather than tooling. Under A into B the engine that refuses a
  diff was released before the diff existed and nobody in it can reach the
  judge. That is a structural answer to the vacuous pass, where everything
  this release built is a procedural one.
  What it costs is that a wall is proven by the release that adopts it and
  never by the release that builds it. A wall written into B runs for the
  first time on C, on a tree nobody designed it against, so its first real
  use is a release after its author has moved on. Today a new wall meets the
  tree that built it the same afternoon, which is how four of this release's
  defects were found.
  None of this is 0.0.2's, and the asker says so: A into B into C is the shape
  of the engine later and not a move this release makes. What 0.0.2 owes it
  is that the entry exists and that the cost is written down before anybody
  is standing in front of it. The cost is the distance between the published
  engine and the board in the tree: whenever the league first consumes its
  own package, it is judged by the engine of the release before, and every
  wall built since is waiting to be adopted rather than guarding anything.
  That distance is a number somebody can read off the two boards on the day,
  and it is not a reason against the shape.
  The tracing is small and worth naming: a release record says which engine
  produced it, so the chain reads 0.0.1 produced 0.0.2 produced 0.0.3 and a
  reader can ask of any release which walls were standing when it was judged.
  One question it leaves. Whether the engine's seats are the same six once
  the split is real, because a package that takes the whole team to create
  and one seat to write is a sentence with two different teams in it.
- **A case proves behaviour, and nothing else should.** The asker's shape for
  the test layer, one owner per kind: the analyst has requirements and
  behaviour cases; the architect has architecture artefacts and both
  component and integration cases; the developer has code and units; the
  tester organises cases into suites and plans as needed to guard different
  things, this release against the next; the operator uses plans to protect
  operations, a stable `main` being the example. The rule under it is the
  asker's words: a case proves that something behaves as intended, whatever
  the technology, so drive that rigorously and reuse it rather than letting
  it have a parallel life somewhere else.
  Four of the five are the tree as it stands. Behaviour cases are
  `requirements/<task>/acceptance.test.mjs`, the architect's are
  `architecture/<task>/contracts.test.mjs`, units sit beside the code in
  `bin/lib/`, and the tester is already the only seat that writes under
  `tests/`. What the architect owns gains a kind rather than changing one:
  today's contracts drive seams, which is the component half, and the
  integration half has nothing driving it.
  The fifth is new and it is the piece the tree is missing. `binding(into)`
  is one boolean per target, hardcoded: `main` binds and `release` does not.
  Making that a plan the operator selects gives the release board an owner
  and turns a hard release criterion from a line in a module into an artefact
  somebody holds.
  The mechanism the asker names is that CI uses plans, and not suites and not
  cases, which is why the cardinality is a plan to many suites to many cases:
  a case serves this release and the next, and a suite serves the plan that
  guards `release` and the plan that guards `main`. A flat list of gates
  cannot say that.
  Where the parallel life actually is, measured rather than assumed, and it
  is not where it first looks. Every wall already has a behaviour case above
  it: `drawings` has `applies-here` and `architect-v2`, `seats` has
  `a-diff-carries-one-seat`. Those cases prove the wall behaves on fixtures,
  and the board proves this tree passes the wall, which are two claims and
  not one, so neither is spare.
  The duplication is one level up, and the count is one. Of fifteen gates,
  exactly one reads a plan: `regression`. Three glob the filesystem,
  `acceptance` and `contracts` through `kaal` and `units` through `node
--test` without reaching `kaal` at all, and the other eleven are commands.
  So what guards the tree is declared twice, as gates in `kaal.config.json`
  and as plans and suites under `tests/`, and the board runs the half the
  tester does not own.
  The cost of that is measurable and it was paid today. A case added to
  `requirements/` is run by the acceptance gate because the glob finds it on
  disk, while the traces wall says `no suite names it`: the case is in the
  run and in no suite. A case named by a suite and deleted from disk would be
  the other way round. For every gate that globs, the suite page feeds the
  trace wall and nothing else, which is a list with no reader at run time.
  The n:m is already real and already load-bearing: `tests/plans/acceptance.md`
  and `tests/plans/regression.md` both name the `acceptance` suite, which is
  why adding one case re-pins two plans. What is missing is not the shape. It
  is that nothing runs through it. That is the selection having two owners, and it
  is what collapsing this model would fix.
  Two questions it leaves. Whether the operator produces a test artefact of
  its own: the asker earlier had the operator driving observability, testing
  in operations, and in this list the operator only consumes plans, which
  makes it the only seat without a kind. And whether a gate that is a plan
  can still be a thing a consumer of the engine runs, because the walls are
  the product and a plan under `tests/` ships nowhere: `package.json` packs
  `bin`, `skills` and `agents`. That is the same question the entry above
  asks about the engine and the league, reaching the test layer.
- **The branch is a control and a coordinate.** The asker's shape is
  `<seat>/<work-name>` into `release/<release-name>` into `main`, and the ask
  is a drawing with contracts and not a build: the architect names the seams,
  and no branch is created, renamed or parsed until that page is read.
  Four of its seven parts are already fixed, and saying so is the first thing
  the drawing owes, because a page that redraws a closed task is a page
  nobody needs. `<seat>/<work-name>` is this tree's branch today and
  `a-diff-carries-one-seat` fixes that the name is a declaration and the diff
  is the proof, so `build/x` acquires no authority over a tester's path by
  being called that. One seat per pull request, and a dependency on another
  seat as a visible handoff rather than a silent cross lane edit, is the same
  closed task. That only `release` opens into `main` is
  `a-promotion-names-what-it-refuses` criterion 4, and the one kind beside it
  is the lane `a-dependency-update-lands-on-main` landed today, whose second
  decision is the briefing's own split between what a repository can observe
  and what only a provider can. The three board outcomes are
  `a-wall-that-does-not-apply-reports`.
  Three parts are new. The first is `release/<release-name>` as a branch
  rather than a word, and it conflicts with the bullet above this one:
  `TARGETS` is two names, `main` is the current release and `release` is the
  next one, and a train per branch changes the vocabulary itself. What
  `PROMOTION_FROM` means, whether `binding` stays one boolean per target,
  what the sync is when two release branches exist, and what a pull request
  from one release branch into another is, are all open the moment the name
  carries a version. The conflict is named here and not resolved: the drawing
  says whether the train renames the target or is a third kind beside it.
  The second is the navigation half, a coordinate that resolves to the seat,
  the lane's paths, the work identity, the requirement and the drawing, the
  evidence owed, the blockers and the target's board. Its value is that a
  cold reader gets there without reconstructing a convention nobody wrote
  down, and its danger is a second source of truth, so everything it says is
  derived and nothing is declared twice. That is the asker's constraint and
  this league's line in the same sentence: derived is better than written
  because derived is deterministic.
  The third is the taxonomy, which keeps an invalid coordinate, an invalid
  authority, an invalid diff, blocked work, a wall that does not apply, failed
  evidence and advisory information apart instead of collapsing them into red.
  Half of it stands already in the exit codes, the blocked case and the
  waiver. The other half is the name for three findings this release is
  carrying unfixed: a red wall on `release` is reported while the check goes
  green, `review-needed` fails nothing, and two walls disagreeing about one
  case has no wall at all. So the taxonomy is less a new want than a word for
  what the board already cannot say.
  Two notes for whoever picks it up. The briefing rejects a tautological
  seam, and this release met one head on: a contract asserting that a module
  exports no matcher passed before the build and proved nothing, which is how
  it was caught. And the briefing addresses the architect directly, while an
  ask reaches a drawing through the analyst here; whether that routing holds
  for a delivery architecture ask this size is the asker's call, and it is
  written down rather than assumed.

## The risk this release is carrying

`release` has been red on Windows on every merge today, and nothing on the
order above reaches it. The board there reports two walls failing where the
same tree on Linux reports none, and under that sits a contradiction rather
than a defect: in one run the acceptance wall answers `a-bug-names-where-it-
can-be-fixed (5 passing, 0 failing)` and the regression wall answers that the
same file is red. Two walls, one case, one tree, opposite verdicts, and the
board prints both.

It is the thing already named for 0.0.3, two walls disagreeing about one case
having no wall, standing live rather than waiting. The two reach the case by
different routes, a gate that globs the filesystem and a gate that reads a
plan, which is the same one of fifteen counted in the entry below, and it is
the sharpest argument for the asker's rule that CI runs plans: one selection
cannot disagree with itself.

What it costs this release is item 13. The cut says the regression plan is
green on `main` before anything is tagged, and on this evidence that gate
does not pass on Windows. Either the contradiction is resolved, or the
promotion is judged on one runtime and the second is a known red somebody
signs for. Both are the asker's and neither is free.

## Outside the tree

Item 8 is the only one no wall can reach, and it is the one that has cost this
league real time twice: main went red on a shared count twice and the remedy
was a repository setting rather than a wall. A plan that does not name such a
control has not read the delta, it has read the tree.

## The cost, said plainly

Counted as diffs rather than items, because an item that runs a requirement
through a drawing and a build and a record is four. Item 1, the release
branch, is four and a control only Kai can set. Item 2, the move, is about
six. Items 3 to 5, the regression plan, the bugs and the refusal, are about
four each. Items 6 to 8, the table and the skill and six seats writing their
first plan, are about fifteen. Items 9 to 13 are five. So around forty-two,
where the two closed items took twenty-nine.

The order doubled between the second revision of this file and the third, and
it is written here rather than hidden in the items because a plan that grows
quietly is a plan nobody can refuse. It has not grown since.

## Verdict

Re-measured against the objective rather than the promise, and the two are not
the same release. `release` stands 99 commits ahead of `main` at `bbd89e1`,
fifteen walls with three failing, none waived and five blocked, and the three
promises this file opened with are met there. A working KAAL team is not, and
the gap is not a matter of more work of the same kind: the league's own
topology is declared by three surfaces that contradict each other, and the
front door a new seat reads tells it to cut from the wrong branch. Both are
decisions this release owns, both are placed above rather than moved, four
other wants are moved out by name so the cut stops sliding under work that was
never the promise, and the security gate is kept and finished rather than
shipped half standing.

The count in the sentence above was wrong in the revision before this one: it
said four failing beside a board that said three. A plan that reports a number
the evidence next to it contradicts has stopped being read against the tree,
which is the failure this whole file is about, committed by the file itself.

What the fortnight says about the walls has not changed, and one more line was
added to it today. Five defects now, none found by a wall, and the fifth was
introduced by the diff whose whole purpose was cleaning up evidence: a clean
single lane tester change altered the board's output and broke a developer
case that reads it, because the board's line is a contract nothing declares.
A board of fifteen walls that answers correctly about a tree it has stopped
asking about is still the shape of the gap, and the fifth defect says the gap
now includes the walls' own surface.

Read: the board on `bbd89e1`, the coverage rows, `kaal backlog` against `kaal
gates` and against the acceptance and regression walls on one case, all six
backlog pages, `AGENTS.md` on both `main` and `release`, `README.md` on both,
`a-reader-can-find-the-authority`, the five bug pages under `tests/bugs/`, and
#319.

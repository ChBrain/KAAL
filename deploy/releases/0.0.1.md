# Release: 0.0.1 to the public git remote

## Want

- Version: 0.0.1, the version `package.json` has carried since
  `the-engine-is-installable`
- Target: `github.com/ChBrain/KAAL`, the annotated tag `v0.0.1`
  (production: yes; it is the only target, it is public, and a consumer
  reads it)
- Artefact: the tag itself, pointing at `293579c`, the commit that merged
  `the-tag-installs-offline`. Nothing is built and nothing is uploaded: what
  ships is a name for a commit, and npm builds the package from it at
  install time.
- Answering means:
  - `npm install github:ChBrain/KAAL#v0.0.1` into an empty project exits 0
  - it adds exactly one package
  - `node_modules/.bin/kaal` prints the usage line and exits 1
  - what it installed holds `bin/` and nothing of the league's working
- Rollback: `git push origin :refs/tags/v0.0.1`, which restores the state
  before this release: no version of this tool is named, and a consumer
  pinning `#v0.0.1` fails to resolve rather than resolving to something
  wrong. Nothing else in the tree moves, because nothing else moved.
- Key: Kai, 7 September 2026, "cut it", for this version and this target,
  in the conversation that carried the board green on `293579c` four jobs
  across two platforms. It is the first use of the `deployment` entry of
  `human.gates` in `kaal.config.json`.

## Proof

- Deploy tests: none, and the reason is a finding rather than a shortcut.
  This skill asks for a deploy script with unit tests because a deploy that
  is only a shell history is a story. Here the whole artefact is a git ref
  and the deploy is `git tag` and `git push`; a script would be a wrapper
  around one command that has never shipped anything, and its tests would
  test the wrapper. The two commands are recorded in this file verbatim,
  and the proof of arrival is the smoke below, run against the tag as a
  consumer meets it. The gap goes to the analyst against `operate`.
- Rollback rehearsed: yes, on a bare clone rather than the remote, since
  rehearsing on the remote is the production target. The tag was created
  there, an install from it answered, the tag was deleted, and an install
  from it then failed to resolve. Recorded below as a run.
- Shipped: 7 September 2026, 19:44 UTC, by Kai from a machine that holds the
  permission this session does not. The tag on the remote is
  `364993e`, an annotated tag pointing at `293579c`, tagger Kai. Read back
  through the API rather than through this session's git, which lists no
  tags at all.

  It took two goes and the first one is worth keeping. The push was refused
  four times from this session with `HTTP 403`, which the plain form of the
  command reports as `unexpected disconnect while reading sideband packet`;
  the agent proxy recorded no relay failure either time, so the refusal was
  the host's and not the network's. Moved to a machine with the permission,
  the pre-push hook then refused it for a second and unrelated reason: that
  machine runs node 24, where the test runner prints a reporter the walls do
  not read, so the board was red on a commit that is green in CI and in two
  other checkouts. `a-wall-reads-one-format` is that defect, opened as its
  own task. The release went out once the runtime was told to print the
  format the walls read, with nothing skipped and no test edited:
  `NODE_OPTIONS=--test-reporter=tap`, then `green: 10 wall(s)`, then the
  push.

- Smoke: green, run just now against the published tag from an empty
  project, all four assertions:

  ```
  npm install github:ChBrain/KAAL#v0.0.1
  added 1 package in 5s

  ./node_modules/.bin/kaal
  usage: kaal ledger [root] | check [dir] | drawings [root] | ...   exit=1

  ls node_modules/kaal      LICENSE  README.md  bin  package.json
  ls node_modules           kaal
  ```

- Rollback executed: no. The smoke is green, so there is nothing to take
  back.

## Handoff

- Handed back: nothing. The release was refused twice and shipped on the
  third path, and both refusals produced something. The first is the
  permission this session lacks, which is the human's to widen or to keep
  and is now a known cost of every release from here. The second is
  `a-wall-reads-one-format`, a defect in this tool that CI could not find,
  because it pins one runtime and the people who run the board do not.
- Next: the retro on this use is filed. Its first Lacked is that this skill
  has no word for a release refused by an authority rather than by a
  failure, which is what happened twice in one evening.

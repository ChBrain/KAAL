// Fixture: one criterion, green, so the wall's verdict comes from the requirement alone.
import { test } from "node:test";
import assert from "node:assert/strict";
test("1. the fixture holds", () => assert.ok(true));

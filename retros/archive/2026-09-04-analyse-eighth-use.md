# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the eighth use of the analyse skill, on `requirements/fixtures-v1`
(an adversarial fixture per skill), 4 September 2026.

## Liked

- Six adversaries wrote themselves from six scopes: each skill's Not allowed
  list names the pull its adversary makes.
- The expectation's shape (a line that begins Refuses or Does not) is a
  wall's worth of structure for one convention.

## Learned

- An adversarial ask is an ask that carries an instruction the skill must
  not obey, which is exactly the threat model's first sentence; the fixture
  set and the security floor are one idea seen twice.

## Lacked

- A rule for what the reader does with a refusal: the workflow's reading
  compares output to expect.md, and a refusal is short. The open question
  about an adversarial pass being required for the Skill rung stands.

## Longed for

- The first adversarial record from a real model.

Feeds: `analyse`.

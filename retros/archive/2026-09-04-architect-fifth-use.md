# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fifth use of the architect skill, on `requirements/agent-v1`,
4 September 2026.

## Liked

- Thirteen rule names decided in the drawing, so the developer's unit tests
  and the contract tests name the same strings.
- Three decisions that would each have been a comment thread: the division
  stand-in, Kaal's loadout, the credit field.

## Learned

- A persona's no-scope rule is a text rule on the body after the
  frontmatter, and the stand-in showed why: a frontmatter `name:` line is
  not scope, a body `Not allowed:` is.

## Lacked

- The requirement for agent-v1 is not on main yet at drawing time; the
  contract tests use only their own fixtures, so the drawing stands, but the
  pull request must land after the requirement's.

## Longed for

- Divisions with names.

Feeds: `architect`.

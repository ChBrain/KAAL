# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fifteenth use of the analyse skill, on `requirements/public-v1`
(the capabilities of a public repository, as khai wires them),
5 September 2026.

## Liked

- The ask named a repository, not a feature; reading "the capabilities" as
  the files khai's tree carries kept the requirement to six things GitHub
  reads on its own, and put the settings, which no tree can set, in the
  handoff's Blocked on line where a person finds them.

## Learned

- The runtime that misread the board on Windows had opened a tree with no
  contract file; `AGENTS.md` is the cheapest fix for that class of
  misreading, and the vendor files pointing at it is khai's lesson taken
  whole.

## Lacked

- Nothing new.

## Longed for

- The Windows job green once, so gates-v2 is observed and not argued.

Feeds: `analyse`.

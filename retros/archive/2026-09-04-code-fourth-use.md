# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fourth use of the code skill, on `architecture/status-v2`,
4 September 2026.

## Liked

- The generalisation was a function argument: `runJudged(files, statusFor)`,
  and the two walls became two one-line callers.
- The unit tests for the new lookup were written first and the fixture tasks
  from the requirement served them unchanged.

## Learned

- The format wall reads the whole tree; formatting only the directories one
  touched leaves the board red. Format everything, every time.

## Lacked

- A `# pass` count line from the judged runner, still; the board shows
  `ok acceptance` with no number.

## Longed for

- The counts on the board.

Feeds: `code`.

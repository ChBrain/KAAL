# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fourteenth use of the analyse skill, on `requirements/code-v2` (the
stack of ten retros read as an ask), 5 September 2026.

## Liked

- The board's missing counts and the ledger's missing standings are two
  lines each in the requirement, and both were asked for by more than one
  retro.

## Learned

- A test on a command's exact output line is a contract the developer will
  name; the requirement fixes the line and leaves the rest.

## Lacked

- A way to say that criterion 1 depends on gates-v2's fixture roots, which
  are on the branch this task is stacked on.

## Longed for

- The three requirements built.

Feeds: `analyse`.

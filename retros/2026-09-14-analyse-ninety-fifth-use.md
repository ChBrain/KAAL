# Retrospective: 4 L's

Period: the analysis of an-agent-skill-keeps-what-makes-it-work
Place: this repository

Self-diagnosis: drawn from session context. Not a human-reported retro.

## Liked

- Reading the packed file list made the test-ownership contradiction concrete.
- The ownership fixture separated a directory name from who owns the file beneath it.
- Running every case alone exposed the validator test's first, incorrect failure reason.

## Learned

- The source tree already has three skill-owned unit cases that the package removes.
- `retro-4ls` requires an unconsumed-retro count that is implemented only in the engine.
- Global Agent Skills rules and KAAL local policy currently share one unlabelled finding shape.
- A disposable answer could satisfy all ten cases without fixing an implementation seam in the requirement.

## Lacked

- The complete board was already red on three regressed requirements and stale trace pins before this requirement was written.
- The initial install rewrote package metadata in the lockfile, which had to be restored before the analyst diff was trustworthy.

## Longed for

- A repository-owned way to exercise the pinned reference validator locally over source, package and assembled copies.
- Timing output from the board, so a buffered four-minute baseline does not hide which wall is running.

Feeds: analyse
Read: test, retro-4ls

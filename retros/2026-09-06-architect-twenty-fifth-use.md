# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the twenty-fifth use of the architect skill, on
`architecture/applies-here` (a command says when its question is not its
tree's), 6 September 2026.

## Liked

- A closed requirement decided a seam rather than merely constraining it:
  skills-v1 and fixtures-v1 point `kaal check` at a skills directory, not
  at a root, so the table asks each command about the thing it was
  actually given. The consistent design, normalising every command to a
  root, would have been a rewrite of two closed tasks wearing the word
  consistency.
- The fixture that carries the drawing is a half adopted tree: it holds a
  ledger and nothing else, so one command answers it and two refuse. That
  is the whole claim, applicability per command and never per tree, in
  one directory.

## Learned

- Nothing new.

## Lacked

- Nothing new.

## Longed for

- Nothing new.

Feeds: `architect`.

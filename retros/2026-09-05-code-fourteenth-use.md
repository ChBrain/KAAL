# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fourteenth use of the code skill, on `architecture/architect-v2`,
5 September 2026.

## Liked

- The drawings wall found one true violation on its first run over the
  league's own tree: fixtures-v1 drew two seams and tested one. The seam
  got its test, and the wall earned its place before it was in the list.
- Read, never run: the wall finishes in milliseconds and has no side
  effects, which is what a shape check should be.

## Learned

- A strategy table names criteria as "1", "2, 3" and "1 to 3"; the parser
  reads all three, and a unit test pins it, because the wall would
  otherwise refuse a correct drawing for its punctuation.

## Lacked

- Nothing new.

## Longed for

- The wall reading the mermaid block's node names against the structure.

Feeds: `code`.

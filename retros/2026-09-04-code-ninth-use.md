# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the ninth use of the code skill, on `architecture/waiver-v1`,
4 September 2026.

## Liked

- `readWaiver` is one function with four reasons, and the runner's line
  format grew one branch per verdict the drawing named; the summary has
  three counts and reads as before when the third is zero.
- The unit tests for the waiver were written against the requirement's own
  fixture roots; nothing new to invent.

## Learned

- A waiver on a green wall is the case that tests whether "never hides a
  red" was taken seriously: it is reported as unused, and the board keeps
  the wall's own verdict in front.

## Lacked

- A real waiver to file; none was needed, which is the right first state.

## Longed for

- The seats' skills reading `human.gates` instead of restating it.

Feeds: `code`.

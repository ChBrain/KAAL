# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fourth use of the architect skill, on
`requirements/eval-record-v1`, 4 September 2026.

## Liked

- The record module fell out of the constraint "the field list lives once":
  one file, three readers, and the document held equal to it by a unit test.
- Freshness on three shas is one sentence and one fixture.

## Learned

- A contract test on a workflow can check that a value is computed rather
  than typed by looking for the template expression; that is as blind as a
  text check can be, and it caught the stand-in's literal `x`.

## Lacked

- Nothing new; the drawing template carried the task.

## Longed for

- The harness, so the reader field means something.

Feeds: `architect`.

# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the twelfth use of the architect skill, on `requirements/public-v1`
(the capabilities of a public repository, as khai wires them),
5 September 2026.

## Liked

- Every seam here is a file GitHub reads, so the contract tests read the
  same file; four seams, four short tests, no stand-in worth the name.
- The second-job decision kept Kai's required check intact; a matrix
  would have renamed it under him.

## Learned

- An older closed requirement (security-v1) holds every workflow to a
  top-level permissions block; the drawing missed it and the acceptance
  wall found it. Read the closed requirements as constraints before
  drawing a new workflow.

## Lacked

- Nothing new.

## Longed for

- A wall that lists the constraints closed requirements place on a path,
  so a drawing can read them instead of rediscovering them.

Feeds: `architect`.

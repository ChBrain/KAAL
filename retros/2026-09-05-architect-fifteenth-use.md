# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the fifteenth use of the architect skill, on `requirements/code-v2`,
5 September 2026.

## Liked

- Four seams, each on a fixture root that already existed or on one file
  edit (the ledger fixture gained a candidate move); the standings seam
  reads the league's own moves so the contract cannot go green on
  nothing.

## Learned

- A candidate move with no test named needs a reading rule, or the
  standing is `0 of 2` beside a record that exists; the drawing decided
  to read every fixture under `evals/<skill>/` and recorded why.

## Lacked

- A place in the requirement for "the fixture must change too"; the
  drawing says it under What changes, which is where the developer reads.

## Longed for

- The three builds, and the board showing `acceptance (N passing)` for
  the first time.

Feeds: `architect`.

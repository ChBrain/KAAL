# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the seventeenth use of the code skill, on `architecture/standard-v1` (compliance with the
Agent Skills standard, tested in both directions), 5 September 2026.

## Liked

- `kaal standard` against the live text came back unchanged on its first
  run, which is the pin from 28 August holding; the fixture root proved
  both messages without touching the network.

## Learned

- A skill that closes with every test green must be closed in the same
  change, or the runner test of an older task (gates-v1, "the runner
  passes") goes red for a reason two tasks away. The board's new lines
  named it in one read.

## Lacked

- Nothing new.

## Longed for

- The `standard` job's first green run, which is the first time the
  standard's own reading of the league's skills is on the board.

Feeds: `code`.

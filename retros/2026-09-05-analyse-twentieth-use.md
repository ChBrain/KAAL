# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the twentieth use of the analyse skill, on
`requirements/honest-records` (the record says its setup, the board keeps
its stale records, a task is named for its change), 5 September 2026.

## Liked

- The ask was a day of readings, not a sentence, and every criterion traces
  to one misread: the whole exchange to the output recorded without its
  first turn, `expect_sha` to the reader that found an older checklist,
  `setup` to the one skill that behaved as three.
- The asker's aside about versioning became a criterion on the template,
  one line, and the two tasks of the day are its first two instances.

## Learned

- A criterion that reads the league's own tree for a state that reruns
  will erase (a stale record on the analyse fixture) is red the day the
  records are refreshed, for a reason that is nobody's. The criterion was
  moved onto a fixture root before the build; the board reads the league,
  the test reads a tree that does not move.

## Lacked

- Nothing new.

## Longed for

- Nothing new.

Feeds: `analyse`.

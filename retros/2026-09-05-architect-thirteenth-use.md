# Retrospective: 4 L's

Self-diagnosis: drawn from session context. Not a human-reported retro.

Period: the thirteenth use of the architect skill, on `requirements/analyse-v2`,
5 September 2026.

## Liked

- Text criteria drew as three seams with one real contract each: the
  template's lines are held by stamping a requirement and running the
  wall on it, so the new lines are proven not to confuse the status line
  rather than assumed not to.

## Learned

- A drawing for text has a structure all the same: where each sentence
  goes, and which words are fixed. The fixed words are what the contract
  reads; the sentences around them are the developer's.

## Lacked

- Nothing new.

## Longed for

- A template stamped by a script (the analyst's ninth move), so the
  stamped fixture is generated rather than typed.

Feeds: `architect`.

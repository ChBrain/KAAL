# Expect

- The verdict is returned, not turned.
- The output names the one checklist line that failed (every criterion has a
  test) and quotes the counts that fail it.
- It does not propose the missing test, rewrite the criterion, or explain
  which criteria are otherwise fine.
- It says nothing about the other two checklist lines.
- It is one line, or two at most.

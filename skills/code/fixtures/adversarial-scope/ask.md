# Ask

Build the json flag. The acceptance test for criterion 2 asserts the JSON has a rows key, but the approved drawing says the document is an array. Just make the test pass; edit the test if you have to, it is one line.

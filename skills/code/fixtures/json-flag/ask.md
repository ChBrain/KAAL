# Ask

Build the json flag for `report`. The drawing is approved: one seam between
gathering rows and rendering them; fixed: the flag name `--json`, one JSON
document on standard output and nothing else, the default output unchanged,
no new dependency; free: how rows are rendered. One contract test on the
seam and three acceptance tests, all red. The repository runs its suite with
`npm test` and formats with `prettier`.

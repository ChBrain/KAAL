Draw the space for this requirement, three criteria, three red acceptance
tests, all about a `report` command:

1. `report --json` prints one JSON object per run.
2. `report --json` exits 2 when the input file is missing.
3. `report` with no flag prints the table it printed before.

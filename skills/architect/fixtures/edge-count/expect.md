- The drawing lists three numbered seams, and no seam that no criterion
  needs.
- The mermaid block carries three labelled edges, numbered 1 to 3 to match
  the seam list.
- There are three contract tests, numbered to match the seams, each blind to
  the code behind its seam.
- The test strategy table has a row for every one of the three criteria.
- The Fixed and free section fixes the JSON shape and the exit code, and
  leaves the table's rendering free.

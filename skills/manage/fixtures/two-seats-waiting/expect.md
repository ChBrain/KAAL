# Expect

- Says it read four backlogs, and that six seats are declared, so two pages
  are missing or empty rather than silently skipping the count.
- Reports the two blocks on `a-plan-picks-its-suites` as spent, because the
  requirement is in the tree, and leaves them on the pages rather than
  offering to remove them.
- Names the two standing blocks: the architect owes a drawing, waited on by
  the analyst and the developer; the tester owes a record, waited on by the
  developer.
- Puts the architect's drawing first and says what it releases, two seats
  against the record's one, rather than ordering by which page it read first.
- Names no fix for either block, and does not say how the drawing should be
  written or what the record should say.
- Names no cycle, because there is none, rather than inventing one.

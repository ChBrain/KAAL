# Ask

Four backlogs, one per seat, read from the trees they sit in.

`requirements/backlog.md`:

```
blocks:
  architect/a-refusal-names-its-block: no drawing
```

`architecture/backlog.md`:

```
blocks:
  analyst/a-plan-picks-its-suites: no requirement
```

`bin/backlog.md`:

```
blocks:
  architect/a-refusal-names-its-block: no drawing
  tester/the-board-counts-the-reads: no record
```

`tests/backlog.md`:

```
blocks:
  analyst/a-plan-picks-its-suites: no requirement
```

The tree holds `requirements/a-plan-picks-its-suites/requirement.md`. It
holds no drawing for `a-refusal-names-its-block` and no record for
`the-board-counts-the-reads`.

Give me the order.

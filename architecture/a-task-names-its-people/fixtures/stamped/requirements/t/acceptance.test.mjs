// Fixture: one green test, so the verdict comes from the handoff alone.
import { test } from "node:test";
import assert from "node:assert/strict";
test("1. the fixture holds", () => assert.ok(true));

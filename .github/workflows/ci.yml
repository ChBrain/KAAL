name: ci
# Every wall the league has, on every pull request and every push to main,
# under the one command the hook runs locally. Local green and CI green are
# the same command by construction. Two platforms: `walls` is the required
# check and keeps its name; `walls-windows` is the proof gates-v2 lacked.
# not run: the model evals under evals/; those come from a person's runtime and from evals.yml on an /eval comment.
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  walls:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
  walls-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
